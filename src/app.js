class FreeAgentTracker extends React.Component {
    
    render() {
        return (
            <div>
                <p>Let's start the code here!</p>
            </div>
        );
    }
}

ReactDOM.render(<FreeAgentTracker />, document.getElementById('app'));